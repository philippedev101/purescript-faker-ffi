import { faker } from "@faker-js/faker";

export const vehicleImpl = () => faker.vehicle.vehicle();

export const manufacturerImpl = () => faker.vehicle.manufacturer();

export const modelImpl = () => faker.vehicle.model();

export const typeImpl = () => faker.vehicle.type();

export const fuelImpl = () => faker.vehicle.fuel();

export const vinImpl = () => faker.vehicle.vin();

export const colorImpl = () => faker.vehicle.color();

export const vrmImpl = () => faker.vehicle.vrm();

export const bicycleImpl = () => faker.vehicle.bicycle();
